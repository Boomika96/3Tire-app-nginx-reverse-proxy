# 3Tire-app-nginx-reverse-proxy
# Write the docker file for both front and backend .
# Refer the server files to get the expose port.
 
# Build the image
"docker build -t image-name:tag"

# write the docker-compose.yaml file for running multiple containers
and using nginx reverse proxy to expose in a port.
1.image pulling
2.data base image
3.revese proxy
4.network

# write the nginx.conf for mapping path using service name.

#Refer all the frontend and backend file for troubleshooting, (db.conf, routes file, service file)
